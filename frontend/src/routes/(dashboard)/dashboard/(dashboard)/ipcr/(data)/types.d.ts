import type { DeleteIPCRSchemanType } from './schema';

type IPCRFormResult = {
	form: any; // You can type this more specifically if needed
	ipcr: Tables<'ipcr'>;
};
type IPCRFormResult = {
	form: any; // You can type this more specifically if needed
	ipcr: Tables<'ipcr'>;
};
